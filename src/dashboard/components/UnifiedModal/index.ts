/**
 * Unified Modal Components
 * 
 * Export all unified modal components and types
 */

export { UnifiedWorkoutModal, type ModalMode } from './UnifiedWorkoutModal';

// Export individual components as default if needed
export { default as UnifiedWorkoutModalComponent } from './UnifiedWorkoutModal'; 